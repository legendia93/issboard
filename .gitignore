# binary hasil build
/issboard
/dist/

# konfigurasi nyata — HANYA contoh yang boleh masuk repo
issboard.yaml
*.local.yaml

# jangan pernah
*.env
.env*
*secret*
*token*
*.key
*.pem

# Konteks operasional mesin nyata — TIDAK PERNAH masuk repo publik ini.
# Berisi hostname, alamat IP, nama pool, dan nama app milik host sungguhan.
# Versi genericnya yang boleh publik: docs/design.md
# Tanpa garis miring di depan: cocok di kedalaman mana pun, termasuk
# docs/konteks-lokal/ — jangan diubah jadi jalur absolut.
konteks-lokal/
dashboard-iss.md
